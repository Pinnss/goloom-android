# Gradle
.gradle/
build/
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
*.ipr
*.iws
captures/
.cxx/

# Native artifacts
app/libs/goloom.aar

# Build outputs (gradle intermediate) и финальные APK.
# Релизные APK раздаём через GitHub Releases, в репо не коммитим
# (старые goloom-v0.1.*.apk остаются в истории, но новые не добавляем).
app/build/outputs/**/*.apk
/goloom-v*.apk
*.aab
*.ap_
*.dex

# Kotlin
.kotlin/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Keystores (никогда не коммитим)
*.keystore
*.jks
keystore.properties
release.jks

# Session-meta — handoff notes, working logs, scratch summaries
# from claude-driven sessions. Stays local, never pushed to the
# public repo.
/HANDOFF*.md
/SESSION*.md
/NOTES*.md
